{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Train Coarse (1 epoch, bs=10)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/coarse/train.py",
            "args": ["--epochs", "1", "--batch_size", "10"],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "justMyCode": false
        }
    ]
}
